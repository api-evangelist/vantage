naftiko: 1.0.0-alpha2
info:
  label: Vantage Cost Management API — Budget Alerts
  description: 'Vantage Cost Management API — Budget Alerts. 5 operations. Lead operation: Vantage Get All Budget Alerts.
    Self-contained Naftiko capability covering one Vantage business surface.'
  tags:
  - Vantage
  - Budget Alerts
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    VANTAGE_API_KEY: VANTAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: cost-management-budget-alerts
    baseUri: https://api.vantage.sh/v2
    description: Vantage Cost Management API — Budget Alerts business capability. Self-contained, no shared references.
    resources:
    - name: budget_alerts
      path: /budget_alerts
      operations:
      - name: getbudgetalerts
        method: GET
        description: Vantage Get All Budget Alerts
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createbudgetalert
        method: POST
        description: Vantage Create a Budget Alert
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: budget_alerts-budget_alert_token
      path: /budget_alerts/{budget_alert_token}
      operations:
      - name: getbudgetalert
        method: GET
        description: Vantage Get a Budget Alert
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: updatebudgetalert
        method: PUT
        description: Vantage Update a Budget Alert
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deletebudgetalert
        method: DELETE
        description: Vantage Delete a Budget Alert
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.VANTAGE_API_KEY}}'
  exposes:
  - type: rest
    namespace: cost-management-budget-alerts-rest
    port: 8080
    description: REST adapter for Vantage Cost Management API — Budget Alerts. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/budget-alerts
      name: budget-alerts
      description: REST surface for budget_alerts.
      operations:
      - method: GET
        name: getbudgetalerts
        description: Vantage Get All Budget Alerts
        call: cost-management-budget-alerts.getbudgetalerts
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createbudgetalert
        description: Vantage Create a Budget Alert
        call: cost-management-budget-alerts.createbudgetalert
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/budget-alerts/{budget-alert-token}
      name: budget-alerts-budget-alert-token
      description: REST surface for budget_alerts-budget_alert_token.
      operations:
      - method: GET
        name: getbudgetalert
        description: Vantage Get a Budget Alert
        call: cost-management-budget-alerts.getbudgetalert
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: updatebudgetalert
        description: Vantage Update a Budget Alert
        call: cost-management-budget-alerts.updatebudgetalert
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletebudgetalert
        description: Vantage Delete a Budget Alert
        call: cost-management-budget-alerts.deletebudgetalert
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: cost-management-budget-alerts-mcp
    port: 9090
    transport: http
    description: MCP adapter for Vantage Cost Management API — Budget Alerts. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: vantage-get-all-budget-alerts
      description: Vantage Get All Budget Alerts
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: cost-management-budget-alerts.getbudgetalerts
      outputParameters:
      - type: object
        mapping: $.
    - name: vantage-create-budget-alert
      description: Vantage Create a Budget Alert
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: cost-management-budget-alerts.createbudgetalert
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: vantage-get-budget-alert
      description: Vantage Get a Budget Alert
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: cost-management-budget-alerts.getbudgetalert
      outputParameters:
      - type: object
        mapping: $.
    - name: vantage-update-budget-alert
      description: Vantage Update a Budget Alert
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: cost-management-budget-alerts.updatebudgetalert
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: vantage-delete-budget-alert
      description: Vantage Delete a Budget Alert
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: cost-management-budget-alerts.deletebudgetalert
      outputParameters:
      - type: object
        mapping: $.
